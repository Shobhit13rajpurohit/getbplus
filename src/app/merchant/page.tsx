import ShellPage from '../ShellPage';

export default function Merchant() {
  return (
    <ShellPage
      title="GetB+ for Merchants"
      description="Accept international cards directly through your existing UPI QR code. No new hardware, zero setup cost."
      phase="Merchant Portal"
    />
  );
}
