import ShellPage from '../ShellPage';

export default function Transport() {
  return (
    <ShellPage
      title="Airport Transport & Cabs"
      description="Book verified, safe, and pre-paid transport directly from major Indian airports. No haggling. No cash needed."
      phase="Phase 2 Feature"
    />
  );
}
